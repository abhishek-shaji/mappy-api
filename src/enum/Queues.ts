export enum Queues {
  UpdateProfileStatistics = 'update.profile.statistics',
  FulfilmentUpdateProgress = 'fulfilment.update.progress',
  GetProfileData = 'profile.get.data',
  GeoGeoLocationTracking = 'geolocation.tracking.session.get',
  IsTrackingSessionStreak = 'geolocation.tracking.session.streak',
  ProfileHandoutReward = 'profile.handout.reward',
}
